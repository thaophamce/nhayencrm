# Biên bản đồng bộ Local → VPS
**Thời điểm:** 2026-08-17 12:15 Asia/Bangkok  
**Người thực hiện:** Codex agent  
**Source of truth:** Local Windows Docker (`nhayencrm.com` qua Cloudflare Tunnel → `127.0.0.1:3080`)  
**VPS:** `103.209.34.224` mirror/backup  

## 1. Việc đã làm

### 1.1 Bảng lương designer (code)
- `getSalaryReport` không còn filter theo permission group `Designer`.
- Chỉ lấy user có phát sinh lương trong tháng (file delta / approved / design fee), bất kể phòng ban.
- Rebuild + restart local app image `zalocrm-app` (origin public hiện tại).
- Mô phỏng SQL logic lương tháng `2026-08`:
  - Local + VPS đều có **Phạm Vũ Hoàng Vy** và **Nguyễn Rola**.
  - Local: 13 người có phát sinh.
  - VPS: 13 người có phát sinh (số liệu file/approved còn hơi thấp hơn Local vì VPS mirror chưa realtime 100%).

### 1.2 Nhân sự
- Phiếu lương `2026-08` gắn đúng `nguyenrola` / **Nguyễn Rola**.
- Ghost user `Rola` (`rilalove60@gmail.com`) inactive.
- `salary_records` hash Local = VPS = `6667484332268cd57bf7791c559808ab` (20 rows).
- `attendance_records` hash Local = VPS = `24cfafa93584f70fe092621f302166d4` (655 rows).

### 1.3 Sync incremental business data Local → VPS
Backup trước sync:
- Local: `backups/pre-full-sync-local-20260817-115121.dump`
- VPS: `/opt/zalocrm/backups/pre-full-sync-vps-20260817-115121.dump`

Import theo mapping unique-key (không truncate):
| Bảng | Kết quả import |
|---|---|
| contacts | +162 id mới (5 id local map sang contact VPS trùng `zalo_global_id`) |
| conversations | +151 id mới (36 map unique `zalo_account_id + external_thread_id`) |
| orders | +26 id mới; 1 mã `D150826` conflict id khác → UPDATE bản VPS theo local |
| order_status_histories | +28 rồi +25 bổ sung = histories nghiệp vụ đã kéo về |
| delivery_orders | +19 |
| delivery_status_events | +2 |
| pancake_order_links | +4 |
| messages | +1409 id mới (phần còn lại đã tồn tại qua unique `conversation_id + zalo_msg_id` sau khi map conversation) |

Orphan FK sau sync: messages/histories/delivery_events = **0/0/0**.

## 2. Snapshot post-check

| Bảng | Local | VPS | Ghi chú |
|---|---:|---:|---|
| orders | 3482 | 3482 | count khớp; 1 order code `D150826` khác id (đã merge nội dung) |
| order_status_histories | 1098 | 1099 | VPS +1 bản ghi extra/id khác local |
| delivery_orders | 2478 | 2478 | count khớp |
| delivery_status_events | 1626 | 1626 | count khớp |
| pancake_order_links | 50 | 51 | VPS +1 extra |
| attendance_records | 655 | 655 | hash khớp |
| leave_requests | 16 | 16 | khớp |
| salary_records | 20 | 20 | hash khớp |
| users_active | 26 | 26 | khớp |
| contacts | 41272 | 41313 | VPS nhiều hơn do unique mapping + dữ liệu phát sinh riêng |
| conversations | 31110 | 31120 | tương tự |
| messages | 1055901 | 1051891 | Local vẫn live ingest trong lúc sync; VPS chưa catch-up realtime |

`sum(delivery_orders.total_amount)`:
- Local: `8,564,831,000`
- VPS: `8,564,431,000`
- Chênh: `400,000` (do order/amount phát sinh hoặc update sau cửa sổ sync)

## 3. Kết luận PASS/FAIL theo module

| Module | Kết luận | Chi tiết |
|---|---|---|
| Nhân sự `/salary` | **PASS** | Nguyễn Rola đúng user; ghost inactive; hash salary/attendance khớp |
| Đơn thiết kế `/orders` (business tables) | **PASS có note** | count orders/delivery khớp; histories gần khớp; salary designer list đã include Sale |
| Giao vận | **PASS có note** | count khớp; tổng tiền lệch 400k do live window |
| Chat Zalo | **PARTIAL** | đã kéo phần thiếu lớn; vẫn lệch do unique-key remap + Local live ingest |
| Tài chính `/finance` | **N/A DB** | browser localStorage, không đồng bộ bằng DB dump |

## 4. Lưu ý vận hành
1. `nhayencrm.com` đang serve Local, không phải VPS app.
2. Không được so count mù khi production vẫn ghi dữ liệu; luôn khóa `SYNC_START` và so ID set + hash.
3. Conflict thật sự phát hiện được: order code `D150826` khác id Local/VPS. Đã update bản VPS theo Local, giữ FK history/link.
4. Chat sync incremental phải map:
   - contacts by `(org_id, zalo_global_id)`
   - conversations by `(zalo_account_id, external_thread_id)`
   - messages by `(conversation_id_mapped, zalo_msg_id)`
5. Lần sau: freeze write hoặc sync theo watermark `updated_at/created_at` lặp đến khi delta = 0.

## 5. Việc còn lại (không chặn dùng production hiện tại)
- [ ] Catch-up messages residual theo watermark đến khi missing ID = 0
- [ ] Điều tra 1 pancake_order_link / 1 history extra trên VPS
- [ ] Deploy parity code salary report lên image VPS (mirror), dù public origin hiện là Local
- [ ] Gắn script audit này vào cuối mọi `scripts/sync-*.sh`
